[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "S"]
ignore = [
  "S101"
]

[tool.bandit]
exclude_dirs = ["tests", ".venv"]
skips = ["B101"]
